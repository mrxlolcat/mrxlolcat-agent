name: CI

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4
    - name: Use Node.js
      uses: actions/setup-node@v4
      with:
        node-version: 20
        cache: 'npm'
    - run: npm ci
    - run: npm run lint
    - run: npm run build
      env:
        # Use secrets if available, otherwise use placeholder for CI
        NEXT_PUBLIC_PROJECT_ID: ${{ secrets.PROJECT_ID || 'dummy' }}
        LIFI_API_KEY: ${{ secrets.LIFI_API_KEY || 'dummy' }}
        PINECONE_API_KEY: ${{ secrets.PINECONE_API_KEY || 'dummy' }}
        OPENROUTER_API_KEY: ${{ secrets.OPENROUTER_API_KEY || 'dummy' }}
        NEYNAR_API_KEY: ${{ secrets.NEYNAR_API_KEY || 'dummy' }}
